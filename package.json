{
  "name": "blogmaker",
  "version": "0.1.0",
  "private": true,
  "description": "WordPress Markdown blog writer for Java education posts with OpenAI and Ollama support.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js"
  },
  "dependencies": {
    "@google/genai": "^1.29.0"
  },
  "engines": {
    "node": ">=18"
  }
}
